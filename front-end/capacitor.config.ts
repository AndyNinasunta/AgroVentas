import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agroventas.main',
  appName: 'AgroVentas',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
